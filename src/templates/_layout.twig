<html>
<head>
  <base href="/">
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="keywords" content={{ texts.head.meta.keywords }}>
  <meta name="author" content={{ texts.head.meta.author }}>
  <meta name="description" content={{ texts.head.meta.description }}>
  <meta name="date" content={{ texts.head.meta.date }}>

  <title>{{texts.head.titlePage}} - {{texts.head.titleMain}}</title>
  <link href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400" rel="stylesheet"> 

  {% for strPathAndFileName in mixFilesStyle %}
  <link href="/{{strPathAndFileName}}" rel="stylesheet">
  {% endfor %}
</head>
<body>
  <div id="eos-wrapper">
    <header>
      {% block header %}{% endblock %}
    </header>

    <main>
      {% block main %}{% endblock %}
    </main>

    <footer>
      {% block footer %}{% endblock %}
    </footer>

    {% for strPathAndFileName in mixFilesScript %}
    <script type="text/javascript" src="/{{strPathAndFileName}}"></script> 
    {% endfor %}
  </div>
</body>
</html>